apiVersion: v1
kind: Secret
metadata:
  name: omniroute-env
type: Opaque
stringData:
  IMAGE: ${IMAGE}
  PORT: "${PORT}"
  REDIS_URL: "${REDIS_URL}"
  NEXT_PUBLIC_BASE_URL: "${NEXT_PUBLIC_BASE_URL}"
  OMNIROUTE_WS_BRIDGE_SECRET: "${OMNIROUTE_WS_BRIDGE_SECRET}"
  DATA_DIR: "${DATA_DIR}"
