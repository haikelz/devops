apiVersion: v1
kind: Service
metadata:
  name: mazanoke
  labels:
    app: mazanoke
spec:
  type: ClusterIP
  selector:
    app: mazanoke
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
