stages:
  - build
  # - deploy

variables:
  DOCKER_TLS_CERTDIR: "/certs"
  DOCKER_DRIVER: overlay2
  IMAGE_NAME: "haikelilham/ryuko-matoi-go"

.ssh_setup: &ssh_setup
  before_script:
    - apk add --no-cache openssh-client
    - mkdir -p ~/.ssh && chmod 700 ~/.ssh
    - echo "$SSH_KEY_VAR" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - echo -e "Host *\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

build-image:
  stage: build
  image: docker:28
  services:
    - docker:28-dind
  before_script:
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  script:
    - docker build -t "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" .
    - docker push "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA"
    - |
      if [ "$CI_COMMIT_BRANCH" = "main" ]; then
        docker tag "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" "$IMAGE_NAME:latest"
        docker push "$IMAGE_NAME:latest"
      fi
    - |
      if [ -n "$CI_COMMIT_TAG" ]; then
        docker tag "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" "$IMAGE_NAME:$CI_COMMIT_TAG"
        docker push "$IMAGE_NAME:$CI_COMMIT_TAG"
      fi
  rules:
    - if: $CI_COMMIT_BRANCH == "main"

#deploy-production:
#image: alpine:latest
#stage: deploy
#timeout: 5 minutes
#<<: *ssh_setup
#variables:
#SSH_KEY_VAR: $VPS_SSH_PRIVATE_KEY
#rules:
#- if: $CI_COMMIT_BRANCH == "main"
#script:
#- ssh -p "$VPS_PORT" "$VPS_USERNAME@$VPS_IP" 'cd /home/ubuntu/ryuko-matoi-go && git pull origin main && docker pull haikelilham/ryuko-matoi-go:latest && docker compose pull whatsapp-bot && docker compose up -d --force-recreate whatsapp-bot'
