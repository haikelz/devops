# Copy to .env, fill in real values, then run ../../deploy-k8s.sh and pick gtcd.
# .env is gitignored; never commit real credentials.

# Public domain of the dashboard (Ingress host + TLS).
DOMAIN=analytics.example.com

# Email for the Let's Encrypt ClusterIssuer (shared/clusterissuer.yaml).
EMAIL=admin@example.com

# gtcd image. Pin an immutable digest for production:
#   docker buildx build --platform linux/amd64 -t ghcr.io/you/gtcd:v1 --push .
#   docker buildx imagetools inspect ghcr.io/you/gtcd:v1   # -> digest
IMAGE=ghcr.io/you/gtcd@sha256:REPLACE_WITH_DIGEST

# In-cluster GoatCounter (deployed from goatcounter.yaml in this directory).
# Use https://your-external-gc-host instead if GoatCounter runs elsewhere.
GOATCOUNTER_URL=http://goatcounter:8080

# GoatCounter API token with read permissions. After the first deploy,
# bootstrap the site/token (see below) and put the token here, then re-run
# deploy-k8s.sh so the Secret is recreated:
#   kubectl exec deploy/goatcounter -- goatcounter db create site \
#     -vhost $DOMAIN -user.email you@example.com -user.password '...' -createdb
#   kubectl exec deploy/goatcounter -- goatcounter db create apitoken \
#     -name gtcd-dashboard -user 1 -perm "count,export,site_read,site_create,site_update"
#   kubectl exec deploy/goatcounter -- goatcounter db query \
#     "UPDATE api_tokens SET permissions='127' WHERE name='gtcd-dashboard'" -format exec
GOATCOUNTER_API_KEY=

# In-cluster Redis (deployed from redis.yaml in this directory).
REDIS_URL=redis://redis:6379

# Not used by the app anymore (kept only because deploy-k8s.sh still requires it).
SESSION_SECRET=unused
